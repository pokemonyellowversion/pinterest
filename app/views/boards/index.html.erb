<h1>All Boards</h1>

<% @boards.each do |board| %>
	<%= board.name %>
	<%= link_to 'Show', board %>
	<%= link_to 'Edit', edit_board_path(board) %>
	<%= link_to 'Destroy', board, method: :delete, data: {
		confirm: 'Are you sure?' } %>
<% end %>

<br>

<%= link_to 'New Board', new_board_path %>
