<% @pins.each do |pin| %>
	<div class="card">
	  <div class="card-image"><%= image_tag pin.image.url %>
	  </div>
	  
	  <div class="card-block">
	    <p class="card-text"><%= pin.description %></p>
	  </div>

<% end %>